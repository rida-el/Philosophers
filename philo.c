#include <philo.h>

int main(int argc, char **argv)
{

    t_utils *utils;

    if (argc != 5 && argc != 6)
    {
        write(1, "error\n", 6);
        return (0);
    }
    utils = malloc();
    
}